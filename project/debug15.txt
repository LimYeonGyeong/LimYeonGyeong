🔥 실제 사용 파일 = /LimYeonGyeong/project/paged_llama/llama/modeling/modeling_llama.py
🔥 LlamaModel.forward 위치 = /LimYeonGyeong/project/paged_llama/llama/utils/utils.py
>>> Multi Request 비교 실행
>>> Baseline multi 로딩 중...
>>> Paged multi 로딩 중...
>>> HF config / state_dict 로딩 중...
>>> 로컬 paged 모델 생성 중...
[LOAD] missing keys: 0
[LOAD] unexpected keys: 0
>>> 로컬 paged 모델 GPU 이동 중...

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call

[MAIN-PREFILL] before call

[MAIN-DECODE STEP 1] before call

[MAIN-DECODE STEP 2] before call

[MAIN-DECODE STEP 3] before call

[MAIN-DECODE STEP 4] before call

[MAIN-DECODE STEP 5] before call

[MAIN-DECODE STEP 6] before call

[MAIN-DECODE STEP 7] before call

[MAIN-DECODE STEP 8] before call

[MAIN-DECODE STEP 9] before call

[MAIN-DECODE STEP 10] before call

[MAIN-DECODE STEP 11] before call

[MAIN-DECODE STEP 12] before call

[MAIN-DECODE STEP 13] before call

[MAIN-DECODE STEP 14] before call

[MAIN-DECODE STEP 15] before call

[MAIN-DECODE STEP 16] before call

[MAIN-DECODE STEP 17] before call

[MAIN-DECODE STEP 18] before call

[MAIN-DECODE STEP 19] before call
[OUTPUT] Multi Request Generation Results (first 5 only)
--- Request 1 ---
### Instruction:
Explain LLM in one short sentence.
### Response:
Long-Lived Memory (LLM) is a technique used in machine learning to store and retrieve
--- Request 2 ---
### Instruction:
Explain transformer attention in one short sentence.
### Response:
Transformer attention is a technique used in neural machine translation (NMT) to model the dependencies between
--- Request 3 ---
### Instruction:
Explain KV cache in one short sentence.
### Response:
KV cache is a type of memory that stores frequently accessed data in memory. It is used to
--- Request 4 ---
### Instruction:
Explain paged attention in one short sentence.
### Response:
Paged attention is a type of attention that is focused on a specific part of the visual field at
--- Request 5 ---
### Instruction:
Explain multi-head attention in one short sentence.
### Response:
Multi-head attention is a technique used in neural machine translation (NMT) to improve the performance

=== Multi Request Result ===
Metric                    | nomal           | Paged           |
Latency (sec)             | 3.3345          | 37.3345         |
Throughput (tok/s)        | 119.96          | 10.71           |
RAM Increase (MB)         | 155.3           | 10.1            |
Peak VRAM (MB)            | 2220.6          | 2148.6          |
Alloc VRAM (MB)           | 2106.3          | 2141.5          |
Reserved VRAM (MB)        | 2258.0          | 2172.0          |
Max Reserved VRAM (MB)    | 2258.0          | 2172.0          |
VRAM/token (KB)           | 5684.72         | 5500.36         |
Context Switch            | 12              | 284             |
Used Blocks               | -               | 98              |
Block Utilization         | -               | 96.08%          |
